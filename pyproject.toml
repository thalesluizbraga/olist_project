[project]
name = "olist-project"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "dbt-bigquery>=1.11.1",
]

[tool.uv.workspace]
members = [
    "dbt_olist",
]
